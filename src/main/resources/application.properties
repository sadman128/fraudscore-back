spring.application.name=fraudscore-back
server.address=0.0.0.0
server.port=5000
spring.mongodb.uri=
spring.mongodb.username=root
spring.mongodb.password=root
spring.mongodb.database=fraudscore

turnstile.secret = 
jwt.token = 

upload.path=uploads

ssl.baseUrl=https://sandbox.sslcommerz.com
ssl.storeId=
ssl.storePass=
app.backendBaseUrl=

app.cors.enabled=false
